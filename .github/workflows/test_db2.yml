# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python package

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13', '3.14']

    steps:
    - uses: actions/checkout@v3

    - name: Start db2 container
      run: |
        docker run -itd --name db2 --privileged=true -p 50000:50000 -e LICENSE=accept -e DB2INST1_PASSWORD=password -e DBNAME=testdb --platform=linux/amd64 icr.io/db2_community/db2

    - name: Wait database create
      run: |
        for t in {0..60}; do
          docker exec -t db2 bash -c "sudo -i -u db2inst1 which db2 2>/dev/null" && break
          sleep 5
        done
        for t in {0..60}; do
          docker exec -t -u db2inst1 db2 bash -c ". ~/sqllib/db2profile && db2 'connect to testdb' 2>/dev/null" && break
          sleep 5
        done

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Test
      run: |
        python test_db2.py
